name: Docker Build and Push
on:
  pull_request:
    branches:
      - master
  push:
    branches:
    - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Login to GHCR and push image
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Extract short commit hash
        run: echo "COMMIT_HASH=$(git rev-parse --short=7 HEAD)" >> $GITHUB_ENV

      - name: Build docker image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          push: true
#          tags: ghcr.io/echoblag/docker-spaceengineers:${{ env.COMMIT_HASH }}, ghcr.io/echoblag/docker-spaceengineers:latest
          tags: ghcr.io/echoblag/docker-spaceengineers:${{ env.COMMIT_HASH }}, ghcr.io/echoblag/docker-spaceengineers:wine11
          file: ./Dockerfile
          platforms: linux/amd64
